import{_ as s,c as i,o as a,b0 as t}from"./chunks/framework.CLL9SSSf.js";const n="/linux-driver-docs/assets/image-20241223135834973.CaVw5JBF.png",r="/linux-driver-docs/assets/image-20241224143356829.DCsKdCP8.png",l="/linux-driver-docs/assets/image-20241224113347161.CIbUlUUb.png",o="/linux-driver-docs/assets/image-20241224150355499.CEVsSMPS.png",p="/linux-driver-docs/assets/image-20241224152029569.BOKhYEBt.png",d="/linux-driver-docs/assets/image-20241224172623454.CsBiSUXy.png",h="/linux-driver-docs/assets/image-20241223150725227.sFfJ1NbK.png",F=JSON.parse('{"title":"LV05-虚拟文件系统VFS","description":null,"frontmatter":{"title":"LV05-虚拟文件系统VFS","date":"2026-01-13T20:35:38.000Z","icon":"famicons:logo-markdown","permalink":"/sdoc/vfs/vfs-fw/126d2cd9c1520ab49d3001c5","index":true,"tags":null,"categories":null,"copyright":false,"keywords":null,"cover":null,"comments":null,"mathjax":null,"top":null,"description":null,"tdoc":{"detailDate":"2026-01-13T20:35:38.171Z","fulluuid":"49d3001c5fc142f59d2646c65d9cbf66","useduuid":"49d3001c5"}},"headers":[],"relativePath":"sdoc/vfs/vfs-fw/126d2cd9c1520ab49d3001c5.md","filePath":"sdoc/10-虚拟文件系统/05-VFS框架/LV05-虚拟文件系统VFS.md","lastUpdated":1769002717000}'),c={name:"sdoc/vfs/vfs-fw/126d2cd9c1520ab49d3001c5.md"};function u(m,e,f,k,b,y){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="lv05-虚拟文件系统vfs" tabindex="-1">LV05-虚拟文件系统VFS <a class="header-anchor" href="#lv05-虚拟文件系统vfs" aria-label="Permalink to &quot;LV05-虚拟文件系统VFS&quot;">​</a></h1><h2 id="一、文件系统" tabindex="-1">一、文件系统 <a class="header-anchor" href="#一、文件系统" aria-label="Permalink to &quot;一、文件系统&quot;">​</a></h2><p>在大概了解过文件系统，这里再深入了解一下。</p><h3 id="_1-什么是文件系统" tabindex="-1">1. 什么是文件系统 <a class="header-anchor" href="#_1-什么是文件系统" aria-label="Permalink to &quot;1. 什么是文件系统&quot;">​</a></h3><p>先来看看什么叫文件系统。直接查百度吧，操作系统中负责管理和存储文件信息的软件机构称为文件管理系统，简称 <a href="https://baike.baidu.com/item/%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/4827215" target="_blank" rel="noreferrer">文件系统</a>。但其实维基百科的解释可能更为精准一些：<a href="https://en.wikipedia.org/wiki/File_system" target="_blank" rel="noreferrer">File system</a>：</p><div class="language-txt vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>In computing, a file system or filesystem (often abbreviated to FS or fs) governs file organization and access. A local file system is a capability of an operating system that services the applications running on the same computer. A distributed file system is a protocol that provides file access between networked computers.</span></span>
<span class="line"><span>A file system provides a data storage service that allows applications to share mass storage. Without a file system, applications could access the storage in incompatible ways that lead to resource contention, data corruption and data loss.</span></span>
<span class="line"><span>There are many file system designs and implementations – with various structure and features and various resulting characteristics such as speed, flexibility, security, size and more.</span></span>
<span class="line"><span>Files systems have been developed for many types of storage devices, including hard disk drives (HDDs), solid-state drives (SSDs), magnetic tapes and optical discs.</span></span>
<span class="line"><span>A portion of the computer main memory can be set up as a RAM disk that serves as a storage device for a file system. File systems such as tmpfs can store files in virtual memory.</span></span>
<span class="line"><span>A virtual file system provides access to files that are either computed on request, called virtual files (see procfs and sysfs), or are mapping into another, backing storage.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>这里就不翻译了，前面学习单片机的时候，肯定会接触到 SPI FLASH、SD 卡等存储介质，我们是可以将文件存放在这些存储设备上的，一开始的时候，我们将数据直接写入到某个地址中，然后读取的时候再从这个地址读出来，这已经很底层了。但是这是很麻烦的，需要很懂底层的原理才行，后来会接触到 fatfs 文件系统，这个系统其实就是一堆的 api 函数，需要我们实现一些对存储介质的初始化、读写相关操作，当我们移植完成后，它会代替我们完成对存储介质的操作，而我们只需要给出一个文件名，它就可以替我们找到这个文件在存储介质中的位置，而不是自己去记文件存放的地址，这对于不懂底层原理的开发者来说就很方便了。</p><p>所以其实文件系统是用于存储和组织文件的一种机制，便于对文件进行方便的查找与访问。文件系统是对文件存储设备的空间进行组织和分配，负责文件存储并对存入的文件进行保护和检索的系统。它负责为用户建立文件，存入、读出、修改、转储文件，控制文件的存取，当用户不再使用时撤销文件等。</p><blockquote><p>简单来说，文件系统是用来管理存储设备（storage devices，storage medium，比如机械硬盘 HDD，SSD，磁带，光盘，SD 卡等）上存储的数据的。</p></blockquote><h3 id="_2-file-system-s-architecture" tabindex="-1">2. file system&#39;s architecture <a class="header-anchor" href="#_2-file-system-s-architecture" aria-label="Permalink to &quot;2. file system&#39;s architecture&quot;">​</a></h3><p>来啊看一下文件系统的结构，还是看 <a href="https://en.wikipedia.org/wiki/File_system" target="_blank" rel="noreferrer">File system</a>，维基百科不一定能访问，这里贴一下原文：</p><div class="language-txt vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A local file system&#39;s architecture can be described as layers of abstraction even though a particular file system design may not actually separate the concepts.[7]</span></span>
<span class="line"><span>The logical file system layer provides relatively high-level access via an application programming interface (API) for file operations including open, close, read and write – delegating operations to lower layers. This layer manages open file table entries and per-process file descriptors.[8] It provides file access, directory operations, security and protection.[7]</span></span>
<span class="line"><span>The virtual file system, an optional layer, supports multiple concurrent instances of physical file systems, each of which called a file system implementation.[8]</span></span>
<span class="line"><span>The physical file system layer provides relatively low-level access to a storage device (e.g. disk). It reads and writes data blocks, provides buffering and other memory management and controls placement of blocks in specific locations on the storage medium. This layer uses device drivers or channel I/O to drive the storage device.[7]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>翻译一下大概就是：</p><blockquote><p>本地文件系统的体系结构可以被描述为抽象层，即使特定的文件系统设计实际上可能没有分离这些概念。</p><p>逻辑文件系统层通过应用程序编程接口（API）为文件操作提供相对高级的访问，包括打开、关闭、读和写——将操作委托给较低的层。这一层管理打开的文件表项和每个进程的文件描述符提供文件访问、目录操作、安全保护等功能。</p><p>虚拟文件系统是一个可选层，它支持物理文件系统的多个并发实例，每个实例称为一个文件系统实现。</p><p>物理文件系统层提供对存储设备（如磁盘）的相对低级的访问。它读取和写入数据块，提供缓冲和其他内存管理，并控制块在存储介质上特定位置的放置。该层使用设备驱动程序或通道 I/O 来驱动存储设备。</p></blockquote><p>其实 VFS 这一层是可选的（optional，即不是必须的）。这个接口对多个并行的物理文件系统实例（每一个都叫做文件系统的实现）提供支持。意思是： <strong>如果只有一个文件系统，那么我可以不要 VFS，但是在有多个文件系统的时候，VFS 就不可或缺了，其在中间起协调、管理作用。</strong></p><p>有一个资料就是参考资料中的【7】<a href="https://www.cs.jhu.edu/~yairamir/cs418/os7/sld004.htm" target="_blank" rel="noreferrer">The File System</a>，是个 PPT 好像，对文件系统有一些说明，这里描述了文件系统的结构：<a href="https://www.cs.jhu.edu/~yairamir/cs418/os7/sld025.htm" target="_blank" rel="noreferrer">File System Architecture</a></p><img src="`+n+'" alt="image-20241223135834973" style="zoom:50%;"><h3 id="_3-file-system-components" tabindex="-1">3. file system components <a class="header-anchor" href="#_3-file-system-components" aria-label="Permalink to &quot;3. file system components&quot;">​</a></h3><ul><li>Device Drivers</li></ul><p>（1）直接与外围设备（磁盘、磁带等）通信。</p><p>（2）负责在设备上启动物理 I/O 操作。</p><p>（3）处理 I/O 请求的完成。</p><p>（4）安排对设备的访问以优化性能。</p><ul><li>Basic File System</li></ul><p>（1）使用特定的设备驱动程序。</p><p>（2）处理与物理设备交换的数据块。</p><p>（3）与磁盘上块的位置有关。</p><p>（4）与主存中的缓冲块有关。</p><ul><li>Logical File System</li></ul><p>负责向用户提供前面讨论的界面，包括：文件访问、目录操作、安全与保护。</p><h3 id="_4-常见的文件系统" tabindex="-1">4. 常见的文件系统 <a class="header-anchor" href="#_4-常见的文件系统" aria-label="Permalink to &quot;4. 常见的文件系统&quot;">​</a></h3><h4 id="_4-1-磁盘文件系统" tabindex="-1">4.1 磁盘文件系统 <a class="header-anchor" href="#_4-1-磁盘文件系统" aria-label="Permalink to &quot;4.1 磁盘文件系统&quot;">​</a></h4><p>磁盘文件系统是指本地主机中实际可以访问到的文件系统，包括硬盘、CD-ROM、DVD、USB 存储器、磁盘阵列等。常见文件系统格式有：</p><ul><li>Ext 系列：</li></ul><p>（1）Ext：专门为 Linux 设计的第一个文件系统，最大支持 2GB 的容量。</p><p>（2）Ext2：由 Rémy Card 设计，用以代替 Ext，最大支持 32TB 的容量，单个文件最大限制为 2TB。</p><p>（3）Ext3：一种日志文件系统，最大支持 32TB 的容量，单个文件最大限制为 16TB。</p><p>（4）Ext4：Theodore Tso 领导的开发团队实现，是许多 Linux 发行版的默认文件系统，提供了良好的性能和可靠性。它支持大文件、大分区、文件压缩、在线碎片整理等特性。最大支持 1EB 的容量，单个文件最大限制也为 16TB。</p><ul><li><p>JFS：IBM 开发的一种文件系统，用于大型数据库和文件服务器，支持文件系统级别的压缩。JFS2 是一种字节级日志文件系统，主要是为满足服务器的高吞吐量和可靠性需求而设计、开发的。</p></li><li><p>XFS：由 SGI 开发，用于高性能的写入密集型应用，如数据库和文件服务器。XFS 支持非常大的文件和卷，以及高并发操作，最大支持 8 exbibytes 的文件系统容量。</p></li><li><p>ReiserFS：一种性能优异的文件系统，特别是在小文件写入和大数量文件系统操作方面表现出色。</p></li><li><p>Btrfs：一种现代化的文件系统，支持高级特性，如数据完整性校验、快照、文件系统级压缩和写入时复制（COW）。</p></li><li><p>ZFS：由 Sun Microsystems 开发，以其数据完整性、可扩展性和性能而闻名。ZFS 是一个复杂的文件系统，提供了类似 RAID 的数据保护特性。</p></li><li><p>FAT 系列：</p></li></ul><p>（1）FAT：一种较旧的文件系统，广泛用于 USB 闪存驱动器和移动存储设备。</p><p>（2）FAT16：采用 16 位的文件分配表，支持最大 2GB 的分区。</p><p>（3）FAT32：采用 32 位的文件分配表，突破了 FAT16 的容量限制，但运行速度相对较慢。</p><p>（4）VFAT：长文件名系统，与 Windows 系统兼容，支持长文件名。</p><ul><li><p>NTFS：微软 Windows 的默认文件系统。Linux 可以通过第三方驱动如 ntfs-3g 来读取和写入 NTFS 文件系统。</p></li><li><p>exFAT：专为闪存存储设计的文件系统，支持大于 4GB 的文件和大容量存储设备。Linux 对 exFAT 的支持正在逐渐改善。</p></li><li><p>ISO9660：光盘的标准化文件系统，广泛用于数据 CD 和 DVD。</p></li><li><p>UDF：通用磁盘格式，常用于光学介质和 DVD 视频。</p></li></ul><h4 id="_4-2-网络文件系统" tabindex="-1">4.2 网络文件系统 <a class="header-anchor" href="#_4-2-网络文件系统" aria-label="Permalink to &quot;4.2 网络文件系统&quot;">​</a></h4><p>网络文件系统是可以远程访问的文件系统，这种文件系统在服务器端仍是本地的磁盘文件系统，客户机通过网络远程访问数据。常见文件系统格式有：</p><ul><li>NFS（Network File System）：由 Sun 开发并发展起来的一项在不同机器、不同操作系统之间通过网络共享文件的技术。</li><li>SMB/CIFS：由微软开发的网络文件系统协议，用于 Windows 和 Linux 之间的文件共享。</li><li>AFP（Apple Filling Protocol）：Apple 文件归档协议，用于 Mac 和 Linux 之间的文件共享。</li><li>WebDAV：基于 HTTP 协议的网络文件系统，允许用户通过 Web 浏览器访问和修改服务器上的文件。</li></ul><h4 id="_4-3-专有-虚拟文件系统" tabindex="-1">4.3 专有/虚拟文件系统 <a class="header-anchor" href="#_4-3-专有-虚拟文件系统" aria-label="Permalink to &quot;4.3 专有/虚拟文件系统&quot;">​</a></h4><p>专有/虚拟文件系统是不驻留在磁盘上的文件系统。常见格式有：</p><ul><li>TMPFS：基于内存的临时文件系统，通常用于存储临时文件。</li><li>PROCFS（Process File System）：进程文件系统，用于管理内存存储目录/proc，存储当前内核运行状态的一系列特殊文件。</li><li>SYSFS：与 PROCFS 一样，也是基于内存的虚拟文件系统，用来管理内存存储目录/sysfs。</li><li>LOOPBACKFS（Loopback File System）：回送文件系统，允许用户将文件当作块设备来处理。</li><li>SWAP：虽然不是传统意义上的文件系统，但 swap 空间在 Linux 中用于虚拟内存，允许系统在物理内存不足时使用磁盘空间作为临时存储。</li></ul><h4 id="_4-4-linux-中支持的文件系统" tabindex="-1">4.4 linux 中支持的文件系统 <a class="header-anchor" href="#_4-4-linux-中支持的文件系统" aria-label="Permalink to &quot;4.4 linux 中支持的文件系统&quot;">​</a></h4><p>我们可以使用下面的命令查看当前 linux 系统支持的文件系统：</p><div class="language-shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/filesystems</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>/proc/filesystems</code> 文件列出了当前内核支持的所有文件系统。</p><h2 id="二、虚拟文件系统" tabindex="-1">二、虚拟文件系统 <a class="header-anchor" href="#二、虚拟文件系统" aria-label="Permalink to &quot;二、虚拟文件系统&quot;">​</a></h2><p>前面其实已经提到了 VFS，它其实就是虚拟文件系统，在学习 linux 的时候会接触到，下面来了解一下。</p><h3 id="_1-vfs-是什么" tabindex="-1">1. VFS 是什么？ <a class="header-anchor" href="#_1-vfs-是什么" aria-label="Permalink to &quot;1. VFS 是什么？&quot;">​</a></h3><p>VFS，全称：Virtual File System，即 <a href="https://baike.baidu.com/item/%E8%99%9A%E6%8B%9F%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/10986803?fromtitle=VFS&amp;fromid=7519887" target="_blank" rel="noreferrer">虚拟文件系统</a>，但是其还有一个称呼：<a href="https://en.wikipedia.org/wiki/Virtual_file_system" target="_blank" rel="noreferrer">virtual filesystem switch</a> （它是维基百科的定义）。</p><p>虚拟文件系统是操作系统内核中的一种抽象层，它提供了一个统一的接口，使得不同的文件系统可以被操作系统内核一致地处理。VFS 允许操作系统支持多种文件系统，而无需为每种文件系统编写特定的代码。通过 VFS，操作系统可以透明地与各种文件系统交互，而无需关心底层文件系统的具体实现细节。</p><blockquote><p>Tips：</p><p>A Virtual File System (VFS) or virtual filesystem switch is an abstract layer on top of a more concrete file system。</p><p>翻译一下就是：VFS 是一个在更具体文件系统之上的抽象层。</p></blockquote><p>需要知道的是 VFS 是一种 <strong>软件机制</strong>，只存在于内存中，每次系统初始化期间 Linux 都会先在内存中构造一棵 VFS 的目录树（也就是源码中的 namespace）。</p><h3 id="_2-它有什么用" tabindex="-1">2. 它有什么用？ <a class="header-anchor" href="#_2-它有什么用" aria-label="Permalink to &quot;2. 它有什么用？&quot;">​</a></h3><p>其实前面解决 VFS 是什么问题的时候就大概可以知道了：</p><p>（1）使得用户应用能够访问不同类型的具体文件系统，比如，一个 VFS 能够用来访问一个本地或者网络存储设备且同时不需要让用户知道区别；</p><p>（2）VFS 可以用来连接（brige）Windows，经典的 Mac OS 以及 Unix 文件系统，使得应用能够访问这些类型的本地文件系统上的文件，而不需要知道他们正在访问的是哪个文件系统；</p><p>（3）VFS 明确了内核（kernel）和具体文件系统之间的接口（或者说契约 contract），所以通过实现对应的契约，我们就能够很轻松的给内核添加对新文件系统的支持；</p><blockquote><p>Tips：</p><p>其实有一个更贴切的描述：To ease the addition of new file systems and provide a generic file API, VFS, a virtual file system layer, was added to the Linux kernel. The extended file system (ext), was released in April 1992 as the first file system using the VFS API and was included in Linux version 0.96c.</p><p>上面这段可以在这里看到：<a href="https://www.diskinternals.com/glossary/ext2/" target="_blank" rel="noreferrer">Ext2 Linux File System | DiskInternals</a> 或者在维基百科：<a href="https://en.wikipedia.org/wiki/Ext2" target="_blank" rel="noreferrer">ext2</a></p></blockquote><h3 id="_3-谁在使用-vfs" tabindex="-1">3. 谁在使用 VFS <a class="header-anchor" href="#_3-谁在使用-vfs" aria-label="Permalink to &quot;3. 谁在使用 VFS&quot;">​</a></h3><p>（1）device drivers，设备驱动（可以看维基百科：<a href="https://en.wikipedia.org/wiki/Device_driver" target="_blank" rel="noreferrer">Device driver</a>)</p><p>（2）filesystems，文件系统</p><h3 id="_4-vfs-和文件系统是什么关系" tabindex="-1">4. VFS 和文件系统是什么关系？ <a class="header-anchor" href="#_4-vfs-和文件系统是什么关系" aria-label="Permalink to &quot;4. VFS 和文件系统是什么关系？&quot;">​</a></h3><p>前面了解文件系统结构的时候其实已经说明了： <strong>如果只有一个文件系统，那么我可以不要 VFS，但是在有多个文件系统的时候，VFS 就不可或缺了，其在中间起协调、管理作用。</strong></p><p>大概可以这么理解吧，就是不同的文件系统，它的实现方式可能多少有些区别，但是一个系统中存储介质可能使用的是不同的文件系统，比如硬盘 1 使用 FAT32，硬盘 2 使用 NTFS，那么他们的接口多少可能会有些区别，但是对于开发人员来说，还要去记这些区别就显得很麻烦，就相当于在文件系统上层又加了一层，将各个接口统一，这一层就是 VFS 了。</p><img src="'+r+'" alt="image-20241224143356829"><h3 id="_5-vfs-在-linux-中的位置" tabindex="-1">5. VFS 在 linux 中的位置 <a class="header-anchor" href="#_5-vfs-在-linux-中的位置" aria-label="Permalink to &quot;5. VFS 在 linux 中的位置&quot;">​</a></h3><p>虚拟文件系统在 linux 架构中位于哪里？</p><img src="'+l+'" alt="image-20241224113347161"><p>Linux 系统的 User 使用 GLIBC（POSIX 标准、GUN C 运行时库）作为应用程序的运行时库，然后通过操作系统，将其转换为系统调用 SCI（system-call interface），SCI 是操作系统内核定义的系统调用接口，这层抽象允许用户程序的 I/O 操作转换为内核的接口调用。VFS 提供了一个抽象层，将 POSIX API 接口与不同存储设备的具体接口实现进行了分离，使得底层的文件系统类型、设备类型对上层应用程序透明。</p><h3 id="_6-两个示例" tabindex="-1">6. 两个示例 <a class="header-anchor" href="#_6-两个示例" aria-label="Permalink to &quot;6. 两个示例&quot;">​</a></h3><h4 id="_6-1-接口适配示例" tabindex="-1">6.1 接口适配示例 <a class="header-anchor" href="#_6-1-接口适配示例" aria-label="Permalink to &quot;6.1 接口适配示例&quot;">​</a></h4><p>用户写入一个文件，使用 POSIX 标准的 write 接口，会被操作系统接管，转调 sys_write 这个系统调用（属于 SCI 层）。然后 VFS 层接受到这个调用，通过自身抽象的模型，转换为对给定文件系统、给定设备的操作，这一关键性的步骤是 VFS 的核心，需要有统一的模型，使得对任意支持的文件系统都能实现系统的功能。这就是 VFS 提供的统一的文件模型（common file model），底层具体的文件系统负责具体实现这种文件模型，负责完成 POSIX API 的功能，并最终实现对物理存储设备的操作。</p><img src="'+o+'" alt="image-20241224150355499"><p>VFS 这一层建模和抽象是有必要的，如果放在 SCI 层会导致操作系统的系统调用的功能过于复杂，易出 bug。那么就只能让底层文件系统都遵循统一实现，这对于已经出现的各种存储设备来说天然就有不同的特性，也是无法实现的。因此 VFS 这样一层抽象是有其必要性的。</p><h4 id="_6-2-跨设备-文件系统示例" tabindex="-1">6.2 跨设备/文件系统示例 <a class="header-anchor" href="#_6-2-跨设备-文件系统示例" aria-label="Permalink to &quot;6.2 跨设备/文件系统示例&quot;">​</a></h4><p>VFS 为不同设备或文件系统间的访问提供了媒介，下面的示意图和代码中，用户通过 cp 命令进行文件的拷贝，对用户来说是不用关心底层是否跨越文件系统和设备的，具体都通过 VFS 抽象层实现对不同文件系统的读写操作。</p><img src="'+p+`" alt="image-20241224152029569"><h2 id="三、vfs-的构成" tabindex="-1">三、VFS 的构成 <a class="header-anchor" href="#三、vfs-的构成" aria-label="Permalink to &quot;三、VFS 的构成&quot;">​</a></h2><h3 id="_1-统一文件模型" tabindex="-1">1. 统一文件模型 <a class="header-anchor" href="#_1-统一文件模型" aria-label="Permalink to &quot;1. 统一文件模型&quot;">​</a></h3><p>文件是存储在硬盘上的，硬盘的最小存储单位叫做扇区 sector，每个扇区存储 512 字节。操作系统读取硬盘的时候，不会一个个扇区地读取，这样效率太低，而是一次性连续读取多个扇区，即一次性读取一个块 block。这种由多个扇区组成的块，是文件存取的最小单位。块的大小，最常见的是 4KB，即连续八个 sector 组成一个 block。</p><p>文件数据存储在块中，那么还必须找到一个地方存储文件的元信息，比如文件的创建者、文件的创建日期、文件的大小等等。这种存储文件元信息的区域就叫做 <code>inode</code>，中文译名为 <code>索引节点</code>，也叫 <code>i节点</code>。</p><blockquote><p>Tips：</p><p>元信息→ inode</p><p>数据→ block</p></blockquote><p>VFS 为了提供对不同底层文件系统的统一接口，需要有一个高度的抽象和建模，这就是 VFS 的核心设计——<strong>统一文件模型</strong>。目前的 Linux 系统的 VFS 都是源于 Unix 家族，因此这里所说的 VFS 对所有 Unix 家族的系统都适用。Unix 家族的 VFS 的文件模型定义了四种对象，这四种对象构建起了统一文件模型。</p><ul><li>superblock：存储文件系统基本的元数据。如文件系统类型、大小、状态，以及其他元数据相关的信息（元元数据）一个超级块对应一个文件系统(已经安装的文件系统类型如 ext2，此处是实际的文件系统，不是 VFS)。之前我们已经说了文件系统用于管理这些文件的数据格式和操作之类的，系统文件有系统文件自己的文件系统，同时对于不同的磁盘分区也有可以是不同的文件系统。</li><li>index node（inode）：保存一个文件相关的元数据。包括文件的所有者（用户、组）、访问时间、文件类型等，但不包括这个文件的名称。文件和目录均有具体的 inode 对应。</li><li>directory entry（dentry）：保存了文件（目录）名称和具体的 inode 的对应关系，用来粘合二者，同时可以实现目录与其包含的文件之间的映射关系。另外也作为缓存的对象，缓存最近最常访问的文件或目录，提示系统性能。</li><li>file：一组逻辑上相关联的数据，被一个进程打开并关联使用。</li></ul><p>统一文件模型是一个标准，各种具体文件系统的实现必须以此模型定义的各种概念来实现。</p><h4 id="_1-1-superblock" tabindex="-1">1.1 Superblock <a class="header-anchor" href="#_1-1-superblock" aria-label="Permalink to &quot;1.1 Superblock&quot;">​</a></h4><p><strong>静态</strong>：superblock 保存了一个文件系统的最基础的元信息，一般都保存在底层存储设备的开头；</p><p><strong>动态</strong>：挂载之后会读取文件系统的 superblock 并常驻内存，部分字段是动态创建时设置的。</p><p>superblock 对应的结构体定义在 <a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/tree/include/linux/fs.h?h=linux-4.19.y&amp;id=e7d2672c66e4d3675570369bf20856296da312c4#n1369" target="_blank" rel="noreferrer">fs.h « linux « include - <em>struct super_block</em></a></p><h4 id="_1-2-index-node" tabindex="-1">1.2 Index node <a class="header-anchor" href="#_1-2-index-node" aria-label="Permalink to &quot;1.2 Index node&quot;">​</a></h4><p><strong>静态</strong>：创建文件系统时生成 inode，保存在具体存储设备上，记录了文件系统的元信息；</p><p><strong>动态：</strong> VFS 在内存中使用 inode 数据结构，来管理文件系统的文件对象，记录了文件对象的详细信息，部分字段与关联的文件对象有关，会动态创建；</p><p><strong>索引节点 inode</strong>：保存的其实是实际的数据的一些信息，这些信息称为“元数据”(也就是对文件属性的描述)。例如：文件大小，设备标识符，用户标识符，用户组标识符，文件模式，扩展属性，文件读取或修改的时间戳，链接数量，指向存储该内容的磁盘区块的指针，文件分类等等。</p><blockquote><p>Tips：数据分成：元数据+数据本身</p><p>注意：inode 有两种，一种是 VFS 的 inode，一种是具体文件系统的 inode。前者在内存中，后者在磁盘中。所以每次其实是将磁盘中的 inode 调进填充内存中的 inode，这样才是算使用了磁盘文件 inode。</p></blockquote><p>inode 怎样生成的？每个 inode 节点的大小，一般是 128 字节或 256 字节。inode 节点的总数，在格式化时就给定(现代 OS 可以动态变化)，一般每 2KB 就设置一个 inode。一般文件系统中很少有文件小于 2KB 的，所以预定按照 2KB 分，一般 inode 是用不完的。所以 inode 在文件系统安装的时候会有一个默认数量，后期会根据实际的需要发生变化。</p><p>inode 号：inode 号是唯一的，表示不同的文件。其实在 Linux 内部的时候，访问文件都是通过 inode 号来进行的，所谓文件名仅仅是给用户容易使用的。当我们打开一个文件的时候，首先，系统找到这个文件名对应的 inode 号；然后，通过 inode 号，得到 inode 信息，最后，由 inode 找到文件数据所在的 block，现在可以处理文件数据了。</p><p>inode 和文件的关系：当创建一个文件的时候，就给文件分配了一个 inode。一个 inode 只对应一个实际文件，一个文件也会只有一个 inode。inodes 最大数量就是文件的最大数量。</p><p>inode 对应的结构体定义在 <a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/tree/include/linux/fs.h?h=linux-4.19.y&amp;id=e7d2672c66e4d3675570369bf20856296da312c4#n593" target="_blank" rel="noreferrer">fs.h « linux « include - <em>struct inode</em></a>：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inode {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//......</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inode_operations	</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">i_op;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//......</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	union</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">		struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hlist_head	i_dentry;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">		struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rcu_head		i_rcu;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	};</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//......</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> file_operations	</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">i_fop;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	/* former -&gt; i_op-&gt; default_file_ops */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//......</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} __randomize_layout;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>i_dentry 字段指定当前 inode 标识的文件对象的名称，也就是 dentry，是一个链表的，因为可能由多个 dentry 都指向这个 inode（硬链接）。然后除了文件的一些权限信息、访问时间、大小等信息之外，最重要的就是记录了 inode 和 file 对象所提供的操作，分别是 i_fop 和 i_op。 inode 在内存中创建后会有 inode cache 进行缓存，并执行延迟的 write back 策略保存到底层存储设备。</p><h4 id="_1-3-directory-entry" tabindex="-1">1.3 Directory entry <a class="header-anchor" href="#_1-3-directory-entry" aria-label="Permalink to &quot;1.3 Directory entry&quot;">​</a></h4><p>目录项：目录项是描述文件的逻辑属性，只存在于内存中，并没有实际对应的磁盘上的描述，更确切的说是存在于内存的目录项缓存，为了提高查找性能而设计。注意不管是文件夹还是最终的文件，都是属于目录项，所有的目录项在一起构成一颗庞大的目录树。</p><p>dentry 是用来记录具体的文件名与对应的 inode 间的对应关系的，同时可以用来实现硬链接、缓存、多级目录等树状文件系统的特性。VFS 的 dentry 设计上就是为了实现整个文件系统树状层次结构的，每个文件系统拥有一个没有父 dentry 的根目录（root dentry），这个 dentry 会被 superblock 引用，用来作为进行树形结构的查找入口。其余的所有 dentry 都是有唯一的父 dentry，并可以由若干个子 dentry。示例：对于一个文件 &quot;/home/user/a”，会存在“/”、“home”、“user”、“a”四个 dentry，依次构成父子关系，每个 dentry 也都有一个 inode 与之关联，存储了具体的数据。</p><p>dentry 没有在磁盘等底层存储设备上存储，是一个动态创建的内存数据结构，主要是为了构建出树状组织结构而设计，用来进行文件、目录的查找。dentry 创建之后会被操作系统进行缓存，目的是为了提升对文件系统进行操作的性能。</p><p>dentry 的结构定义在 <a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/tree/include/linux/dcache.h?h=linux-4.19.y&amp;id=e7d2672c66e4d3675570369bf20856296da312c4#n88" target="_blank" rel="noreferrer">dcache.h « linux « include - <em>struct dentry</em></a>。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dentry {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//......</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d_inode;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">		/* Where the name belongs to - NULL is</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">					 * negative */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> d_iname</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[DNAME_INLINE_LEN];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	/* small names */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	/* Ref lookup also touches following */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lockref d_lockref;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	/* per-dentry lock and refcount */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">	const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dentry_operations </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d_op;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//......</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} __randomize_layout;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>其中最重要的有两个字段，一个是 d_inode 指针指向了当前 dentry 关联的 inode。另一个就是 d_op 字段，指向了一系列 dentry 支持的操作的集合。</p><ul><li>dentry 在需要使用时动态创建，并会被缓存。每个 dentry 有三种状态：</li></ul><p>（1）used：与一个 inode 关联，正处于被 VFS 使用的状态，不能被损坏和丢弃</p><p>（2）unused：与 inode 关联，但处于被缓存状态，没有被 VFS 使用</p><p>（3）negative：没有与具体的 inode 关联（相当于是一个无效的路径）</p><ul><li>dentry 由于会被动态创建，为了提升系统性能，设计了一个 dentry cache 进行缓存，包括三个部分：</li></ul><p>（1）used dentries 链表：记录每个正在使用的 dentry，将其关联的 inode 的 i_dentry 字段指向的 dentry 链表连接起来形成一个 dentry 链表的链表</p><p>（2）LRU 双向环链表：用于维护 unused 和 negative 状态的 dentry 对象，从头部插入，离头部越近就是最近访问过的。当需要删除 dentry 时，从队列尾部删除最旧的 dentry</p><p>（3）hash table 和 hash function：用来快速查询一个给定的路径到 dentry 对象</p><h4 id="_1-4-file" tabindex="-1">1.4 File <a class="header-anchor" href="#_1-4-file" aria-label="Permalink to &quot;1.4 File&quot;">​</a></h4><p>文件对象描述的是进程已经打开的文件。因为一个文件可以被多个进程打开，所以一个文件可以存在多个文件对象。但是由于文件是唯一的，那么 inode 就是唯一的，目录项也是定的！</p><p>进程其实是通过文件描述符来操作文件的，注意每个文件都有一个 32 位的数字来表示下一个读写的字节位置，这个数字叫做文件位置。一般情况下打开文件后，打开位置都是从 0 开始，除非一些特殊情况。Linux 用 file 结构体来保存打开的文件的位置，所以 file 称为打开的文件描述。file 结构形成一个双链表，称为系统打开文件表。</p><p>文件对象是打开一个具体文件之后创建的一个内存数据结构，与具体的进程和用户相联系。一个文件对象包括的内容就是编程语言支持设置的各种文件打开的 flag、mode，文件名称、当前的偏移等，其中非常重要的一个字段就是 f_op，指向了当前文件所支持的操作集合。</p><p>file 结构体定义在 <a href="https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/tree/include/linux/fs.h?h=linux-4.19.y&amp;id=e7d2672c66e4d3675570369bf20856296da312c4#n891" target="_blank" rel="noreferrer">fs.h « linux « include - <em>struct file</em></a>。</p><h4 id="_1-5-vfs-抽象组件关系" tabindex="-1">1.5 VFS 抽象组件关系 <a class="header-anchor" href="#_1-5-vfs-抽象组件关系" aria-label="Permalink to &quot;1.5 VFS 抽象组件关系&quot;">​</a></h4><img src="`+d+'" alt="image-20241224172623454"><p>每个打开的文件对用户来说有一个文件描述符，也就是 VFS 抽象的 file object。file object 指向一个 dentry，dentry 指向新的 dentry 或一个 inode，inode 最终代表了一个具体存储设备上的数据。</p><h3 id="_2-vfs-由什么构成" tabindex="-1">2. VFS 由什么构成？ <a class="header-anchor" href="#_2-vfs-由什么构成" aria-label="Permalink to &quot;2. VFS 由什么构成？&quot;">​</a></h3><p>上面已经了解了统一文件模型，现在再来看这个问题，大概就好理解一点了。这里主要参考的是这个文章【8】<a href="https://aeb.win.tue.nl/linux/lk/lk-8.html" target="_blank" rel="noreferrer">The Linux kernel: The Linux Virtual File System</a> 和【9】<a href="http://www.linux-tutorial.info/?page_id=426" target="_blank" rel="noreferrer">虚拟文件系统 |Linux 教程</a>。</p><img src="'+h+'" alt="image-20241223150725227"><p>VFS 用 <strong>superblocks 和 inodes</strong> 来描述系统文件（和 EXT2 的文件系统使用 superblocks，inodes 非常相似）。其中 inodes 用来描述系统内的文件和目录。</p><p>每个文件系统在初始化的时候，它会用 VFS 注册它自己（我理解的是：<strong>每个真实文件系统挂载上来时，都会在 VFS 上登记备案</strong> ），这通常发生在操作系统在系统引导（system boot time）时初始化它自己的过程。</p><blockquote><p>【9】<a href="http://www.linux-tutorial.info/?page_id=426" target="_blank" rel="noreferrer">虚拟文件系统 |Linux 教程</a> 中有这么一段：</p><p>The real file systems are either built into the kernel itself or are built as loadable modules. File System modules are loaded as the system needs them, so, for example, if the VFAT file system is implemented as a kernel module, then it is only loaded when a VFAT file system is mounted. When a block device based file system is mounted, and this includes the root file system, the VFS must read its superblock. Each file system type’s superblock read routine must work out the file system’s topology and map that information onto a VFS superblock data structure. The VFS keeps a list of the mounted file systems in the system together with their VFS superblocks. Each VFS superblock contains information and pointers to routines that perform particular functions. So, for example, the superblock representing a mounted EXT2 file system contains a pointer to the EXT2 specific inode reading routine. This EXT2 inode read routine, like all of the file system specific inode read routines, fills out the fields in a VFS inode. Each VFS superblock contains a pointer to the first VFS inode on the file system. For the root file system, this is the inode that represents the &quot;/&quot; directory. This mapping of information is very efficient for the EXT2 file system but moderately less so for other file systems.</p></blockquote><p>这一段说的大概是：</p><p>（1）真实文件系统要么在内核中构建（意思就是直接装在内核里面成为一部分了），要么以可加载模块的方式被构建。文件系统模块只有在系统需要他们的时候，才会被加载。比如，VFAT 文件系统被实现成一个内核模块，只有当一个 VFAT 文件系统被挂载的时候，它才会被加载。</p><p>（2）当一个基于块设备的文件系统被加载，且这包含根文件系统，VFS 就必须读取它的 superblocks（应该是叫超级块），每个文件系统类型的超级块 read routine 必须算出文件系统的拓扑结构，并且把这个信息映射到 VFS 的超级块数据结构中。</p><p>（3）VFS 保持着一个在系统（意思就是操作系统）中被挂载文件系统的列表以及他们的 VFS 超级块。每个 VFS 超级块包含到 routines（指令集 a list of instructions that enable a computer to perform a particular task）的信息和指针。</p><p>那上图的 cache 和 directory cache 又是什么呢？</p><blockquote><p>【9】<a href="http://www.linux-tutorial.info/?page_id=426" target="_blank" rel="noreferrer">虚拟文件系统 |Linux 教程</a> 中有这么一段：</p><p>As the system’s processes access directories and files, system routines are called that traverse the VFS inodes in the system.</p><p>For example, typing ls for a directory or cat for a file cause the the Virtual File System to search through the VFS inodes that represent the file system. As every file and directory on the system is represented by a VFS inode, then a number of inodes will be being repeatedly accessed. These inodes are kept in the inode cache which makes access to them quicker. If an inode is not in the inode cache, then a file system specific routine must be called in order to read the appropriate inode. The action of reading the inode causes it to be put into the inode cache and further accesses to the inode keep it in the cache. The less used VFS inodes get removed from the cache.</p><p>All of the Linux file systems use a common buffer cache to cache data buffers from the underlying devices to help speed up access by all of the file systems to the physical devices holding the file systems.</p><p>This buffer cache is independent of the file systems and is integrated into the mechanisms that the Linux kernel uses to allocate and read and write data buffers. It has the distinct advantage of making the Linux file systems independent from the underlying media and from the device drivers that support them. All block structured devices register themselves with the Linux kernel and present a uniform, block based, usually asynchronous interface. Even relatively complex block devices such as SCSI devices do this. As the real file systems read data from the underlying physical disks, this results in requests to the block device drivers to read physical blocks from the device that they control. Integrated into this block device interface is the buffer cache. As blocks are read by the file systems they are saved in the global buffer cache shared by all of the file systems and the Linux kernel. Buffers within it are identified by their block number and a unique identifier for the device that read it. So, if the same data is needed often, it will be retrieved from the buffer cache rather than read from the disk, which would take somewhat longer. Some devices support read ahead where data blocks are speculatively read just in case they are needed.</p><p>The VFS also keeps a cache of directory lookups so that the inodes for frequently used directories can be quickly found.</p><p>As an experiment, try listing a directory that you have not listed recently. The first time you list it, you may notice a slight pause but the second time you list its contents the result is immediate. The directory cache does not store the inodes for the directories itself; these should be in the inode cache, the directory cache simply stores the mapping between the full directory names and their inode numbers.</p></blockquote><p>大概就是再说：</p><p>（1）所有的 linux 文件系统都使用一个常用的 buffer cache（缓冲区缓存）来缓存来自底层设备的 data buffers，这样可以加速文件系统对保有这个文件系统的物理设备的访问。</p><p>（2）VFS 也保有了一个文件夹查找（directory lookups）缓存（cache），以便经常被使用的文件夹的 inodes 能够被快速找到。（所以，通常在 ls 一个最近从没打开或者 ls 过的文件夹时，会有一点点停顿，但第二次就会立马显示结果。）</p><p>（3）文件夹缓存不存储针对文件夹本身的 inodes（这些应该放在 inodes 缓存中），它存储了整个文件夹名字和他们的 inode 号之间的映射。</p><p>（4）系统中的每个文件或者文件夹都是用一个 VFS inode 表示的，放在 inode cache 里面的 inode 会被更快的访问，如果某个 inode 不在 cache 中，那么一个文件系统的指定 routine（指令集）就会被调用来读取对应的 inode。读取这个 inode 使得它被放进缓存中，进一步的访问会将它保持在缓存中。此外，更少被用到的 VFS inodes 会从 cache 中移除。</p><blockquote><p>参考资料</p><p>【1】<a href="https://github.com/sumumm/linux-0.12/blob/master/docs/%E7%AC%AC12%E7%AB%A0-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/%E7%AC%AC12%E7%AB%A0-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F.md" target="_blank" rel="noreferrer">linux-0.12/docs/第 12 章-文件系统/第 12 章-文件系统.md at master · sumumm/linux-0.12 · GitHub</a></p><p>【2】<a href="https://blog.csdn.net/qq_35995514/article/details/120992822" target="_blank" rel="noreferrer">【Linux】VFS 虚拟文件系统 详解-CSDN 博客</a></p><p>【3】<a href="https://linux.cn/article-10884-1.html" target="_blank" rel="noreferrer">技术|详解 Linux 中的虚拟文件系统</a></p><p>【4】[<a href="https://blog.csdn.net/weixin_39278265/article/details/88839027" target="_blank" rel="noreferrer">高级操作系统] VFS 详解（虚拟文件系统）_操作系统 vfs 层-CSDN 博客</a></p><p>【5】<a href="https://www.cnblogs.com/smartjourneys/p/7258226.html" target="_blank" rel="noreferrer">Linux 文件系统 1---概述 - jasonactions - 博客园</a></p><p>【6】<a href="https://blog.csdn.net/chungle2011/article/details/80281226" target="_blank" rel="noreferrer">Linux 文件系统 2---VFS 的四个主要对象_vfs 主要由哪几种类型的对象组成-CSDN 博客</a></p><p>【7】<a href="https://www.cs.jhu.edu/~yairamir/cs418/os7/sld004.htm" target="_blank" rel="noreferrer">The File System</a></p><p>【8】<a href="https://aeb.win.tue.nl/linux/lk/lk-8.html" target="_blank" rel="noreferrer">The Linux kernel: The Linux Virtual File System</a></p><p>【9】<a href="http://www.linux-tutorial.info/?page_id=426" target="_blank" rel="noreferrer">虚拟文件系统 |Linux 教程</a></p><p>【10】<a href="https://blog.csdn.net/jinking01/article/details/90669534" target="_blank" rel="noreferrer">linux 的 VFS 详解-CSDN 博客</a></p><p>【11】<a href="https://developer.ibm.com/tutorials/l-linux-filesystem/?mhsrc=ibmsearch_a&amp;mhq=Anatomy%20of%20the%20Linux%20file%20system" target="_blank" rel="noreferrer">Anatomy of the Linux file system - IBM Developer</a></p><p>【12】<a href="https://blog.csdn.net/xie__jin__cheng/article/details/142729227" target="_blank" rel="noreferrer">文件系统、常见类型及其特点_文件系统类型-CSDN 博客</a></p></blockquote>',150)])])}const S=s(c,[["render",u]]);export{F as __pageData,S as default};
